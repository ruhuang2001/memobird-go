# Memobird Playground Configuration

memobird:
  access_key: "your-access-key-here"
  device_id: "your-device-id-here"
  user_id: 0
  timeout: 30s
  # base_url: "https://open.memobird.cn"

storage:
  db_path: "./memobird.db"
